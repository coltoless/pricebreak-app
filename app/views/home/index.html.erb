<div class="landing-container">
  <div class="hero-section">
    <h1 class="main-title">priceBreak</h1>
    <h2 class="tagline">Tickets at Your Price!</h2>
    
    <div class="categories">
      <a href="#" class="category-card">
        <span class="category-icon">🎵</span>
        <span class="category-name">Music</span>
      </a>
      <a href="#" class="category-card">
        <span class="category-icon">🏟️</span>
        <span class="category-name">Sports</span>
      </a>
      <a href="#" class="category-card">
        <span class="category-icon">🎭</span>
        <span class="category-name">Comedy</span>
      </a>
      <a href="#" class="category-card">
        <span class="category-icon">✈️</span>
        <span class="category-name">Flights</span>
      </a>
      <a href="#" class="category-card">
        <span class="category-icon">🎪</span>
        <span class="category-name">All Events</span>
      </a>
    </div>

    <div class="cta-buttons">
      <a href="#" class="cta-button primary">Find Tickets</a>
      <a href="#" class="cta-button secondary">How It Works</a>
    </div>
  </div>
</div>

<style>
  .landing-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
    color: white;
    text-align: center;
    padding: 2rem;
  }

  .hero-section {
    max-width: 800px;
  }

  .main-title {
    font-size: 4rem;
    font-weight: 800;
    margin-bottom: 1rem;
    letter-spacing: -0.05em;
  }

  .tagline {
    font-size: 2rem;
    font-weight: 400;
    margin-bottom: 2rem;
    opacity: 0.9;
  }

  .categories {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 1rem;
    margin: 2rem 0;
    padding: 0 1rem;
  }

  .category-card {
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border-radius: 1rem;
    padding: 1.5rem 1rem;
    text-decoration: none;
    color: white;
    transition: transform 0.2s, background-color 0.2s;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .category-card:hover {
    transform: translateY(-4px);
    background: rgba(255, 255, 255, 0.2);
  }

  .category-icon {
    font-size: 2rem;
  }

  .category-name {
    font-size: 1rem;
    font-weight: 500;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-top: 2rem;
  }

  .cta-button {
    padding: 1rem 2rem;
    border-radius: 0.5rem;
    font-size: 1.125rem;
    font-weight: 600;
    text-decoration: none;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }

  .primary {
    background-color: white;
    color: #4f46e5;
  }

  .secondary {
    background-color: transparent;
    color: white;
    border: 2px solid white;
  }

  @media (max-width: 640px) {
    .main-title {
      font-size: 3rem;
    }

    .tagline {
      font-size: 1.5rem;
    }

    .categories {
      grid-template-columns: repeat(2, 1fr);
    }

    .cta-buttons {
      flex-direction: column;
    }
  }
</style> 