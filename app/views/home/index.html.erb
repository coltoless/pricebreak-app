<div class="coming-soon">
  <div class="content">
    <h1>priceBreak</h1>
    <p class="tagline">Tickets at Your Price</p>
    <p class="message">We're working hard to bring you the best ticket deals. Come back soon!</p>
    
    <div class="notification-form">
      <h2>Get Notified When We Launch</h2>
      <form action="/notify" method="post">
        <div class="input-group">
          <input type="email" name="email" placeholder="Enter your email" required>
          <button type="submit">Notify Me</button>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
  .coming-soon {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
    color: white;
    text-align: center;
    padding: 2rem;
  }

  .content {
    max-width: 600px;
    width: 100%;
  }

  h1 {
    font-size: 4rem;
    font-weight: 700;
    margin-bottom: 1rem;
    letter-spacing: -0.05em;
  }

  .tagline {
    font-size: 1.5rem;
    margin-bottom: 2rem;
    opacity: 0.9;
  }

  .message {
    font-size: 1.25rem;
    margin-bottom: 3rem;
    opacity: 0.8;
  }

  .notification-form {
    background: rgba(255, 255, 255, 0.1);
    padding: 2rem;
    border-radius: 1rem;
    backdrop-filter: blur(10px);
  }

  .notification-form h2 {
    font-size: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .input-group {
    display: flex;
    gap: 1rem;
    max-width: 500px;
    margin: 0 auto;
  }

  input[type="email"] {
    flex: 1;
    padding: 1rem;
    border: none;
    border-radius: 0.5rem;
    font-size: 1rem;
    background: rgba(255, 255, 255, 0.9);
  }

  button {
    padding: 1rem 2rem;
    border: none;
    border-radius: 0.5rem;
    background: white;
    color: #4f46e5;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, background-color 0.2s;
  }

  button:hover {
    transform: translateY(-2px);
    background: #f8f9fa;
  }

  @media (max-width: 640px) {
    h1 {
      font-size: 3rem;
    }

    .input-group {
      flex-direction: column;
    }

    button {
      width: 100%;
    }
  }
</style> 